import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  modal: {
    paddingVertical: 0,
    marginHorizontal: 0,
  },
  content: {
    width: '100%',
    height: 600,
    bottom: 0,
    position: 'absolute',
    borderRadius: 5,
    backgroundColor: '#fff',
  },
})

export default styles